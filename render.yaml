services:
  - type: web
    name: deribit-mcp-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node mcp-sse-oauth-server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production